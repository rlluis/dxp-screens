<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="rlluis.liferay.dxpbundle">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.liferay.mobile.screens.MenuActivity"
            android:label="@string/title_activity_menu"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".MenuActivity"
            android:label="@string/title_activity_menu"
            android:parentActivityName=".MainActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="rlluis.liferay.dxpbundle.MainActivity" />
        </activity>
        <activity android:name=".EventsActivity" />
        <activity android:name=".FormActivity" />
        <activity android:name=".UserPortrait" />
        <activity android:name=".ListaEventos" />
        <activity android:name=".ListaFormularios" />
        <activity android:name=".ListaBlogs" />
        <activity android:name=".BlogActivity"></activity>
    </application>

</manifest>